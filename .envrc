# Load the .env file
dotenv

# Additional project-specific settings can be added here
# For example:
# export FLASK_ENV=development
# export FLASK_APP=app.py

# You can also set PATH or other environment variables if needed
# export PATH=$PATH:$PWD/bin

# Print a message to confirm the file has been loaded
echo "Project environment loaded"